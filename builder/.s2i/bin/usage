#!/bin/bash -e
cat <<EOF
Esta es la imagen nginx-builder:
Para usarla se requiere previamente instalar S2I: https://github.com/openshift/source-to-image

Correr build de la imagen:

s2i build git://<source code> nginx-builder <application image>

Correr run de la imagen:
docker run <application image>
EOF
